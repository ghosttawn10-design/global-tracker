{
  "buildCommand": "pnpm run build",
  "outputDirectory": "artifacts/globaltrack/dist/public",
  "cleanUrls": true,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
